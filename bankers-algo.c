#include <stdio.h>
int main()
{
    int processCount;
    int resourcesCount;

    printf("\nEnter the number process: ");
    scanf("%d", &processCount);
    printf("\nEnter the number resources: ");
    scanf("%d", &resourcesCount);
}